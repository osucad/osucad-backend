ktor:
  application:
    modules:
      - com.osucad.server.websocketGateway.ApplicationKt.module
  deployment:
    port: 8080

osucad:
  redis:
    hostname: $REDIS_HOSTNAME
    port: "$REDIS_PORT:6379"